import 'package:flutter/material.dart';

class OfficalScreen extends StatelessWidget {
  const OfficalScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text('Up comming'),
      ),
    );
  }
}
